<%= form_with(model: business_entity, local: true) do |form| %>
  <% if business_entity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(business_entity.errors.count, "error") %> prohibited this business_entity from being saved:</h2>

      <ul>
      <% business_entity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :founded %>
    <%= form.date_select :founded %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
